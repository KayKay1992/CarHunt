Navigation and header section

we created a navbar component in component folder. this is where we coded our navbar component which contains the logo the header titles, the sign in and sign up button and a call to action button sell a car. we created a logo component index.tsx we passed the logo component into the navbar omponent. then we passed the navbar component into the layout file.
